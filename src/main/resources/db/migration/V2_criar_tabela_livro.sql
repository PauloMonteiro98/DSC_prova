create table livro (
    id int auto_increment primary key,
    titulo varchar(200) not null,
    nacionalidade varchar(200) not null,
    ISBN varchar(200) not null,
    anopublicacao varchar(200) not null
);